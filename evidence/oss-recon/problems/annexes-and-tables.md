# Problem: annexes and tables

## Problem statement

Address content inside annexes, tables and image-captured structures
with provable locators — the region where BOE legal content is least
text-structured.

## RegDelta surface

- `anejo:*` locator keys and root resets in
  `src/regdelta/operations.py` — anejo vs norma ownership is a
  classified wrong-locator pattern (G2.2 §47)
- `ACQUISITION_FAILURE` on the G2.2 sealed run: an annex image was
  reached but never captured — annex content is the frontier of the
  evidence-capture layer, not only of parsing
- State-code families (`FI|FC|PI|PC|PA|UEM|AVE`) live largely inside
  annex tables

## Candidates

- `projects/indigo.md` — component/annex modeling in AKN
- `projects/legaldocml-akn.md` — AKN `hcontainer`/table structures
- `projects/words-to-data.md` — hierarchical path coverage of
  non-linear structures

## Open question

Do upstream models give annex/table cells provable locators tied to
source bytes, or only logical addresses inside a normalized tree —
and how do they handle content captured only as images?

## Verdict

PENDING — filled when all candidate fichas are evaluated.
